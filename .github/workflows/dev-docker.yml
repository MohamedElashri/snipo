name: Testing Branching Workflow

on:
  push:
    branches: [dev]
    paths-ignore:
      - 'tui/**'
      - 'extension/**'
      - '**.md'
      - 'docs/**'
      - '.github/**'
      - 'LICENSE'
      - '.vscode/**'
      - 'Makefile'
      - 'package.json'
      - 'package-lock.json'
      - 'scripts/**'

permissions:
  packages: write
  contents: read

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  test:
    name: Test Before Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # sanad: ref=v6.0.1

      - name: Setup Go
        uses: actions/setup-go@4dc6199c7b1a012772edbd06daecab0f50c9053c # sanad: ref=v6.1.0
        with:
          go-version: '1.25'
          cache: true

      - name: Run tests
        run: go test -v -race ./...

  build-and-push-testing:
    name: Build and Push Testing Image (${{ matrix.name }})
    runs-on: ubuntu-latest
    needs: test
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: standard
            dockerfile: Dockerfile
            tag_suffix: ""
          - name: hardened
            dockerfile: Dockerfile.hardened
            tag_suffix: "-hardened"
    steps:
      - name: Checkout
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # sanad: ref=v6.0.1

      - name: Set up QEMU
        uses: docker/setup-qemu-action@c7c53464625b32c7a7e944ae62b3e17d2b600130 # sanad: ref=v3.7.0

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f # sanad: ref=v3.12.0

      - name: Login to GitHub Container Registry
        uses: docker/login-action@5e57cd118135c172c3672efd75eb46360885c0ef # sanad: ref=v3.6.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Get commit info
        id: commit
        run: |
          echo "SHORT_SHA=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT
          echo "DATE=$(date -u +'%Y%m%d')" >> $GITHUB_OUTPUT
          echo "IMAGE_NAME_LOWER=$(echo '${{ env.IMAGE_NAME }}' | tr '[:upper:]' '[:lower:]')" >> $GITHUB_OUTPUT

      - name: Build and push
        uses: docker/build-push-action@10e90e3645eae34f1e60eeb005ba3a3d33f178e8 # sanad: ref=v6
        with:
          context: .
          file: ${{ matrix.dockerfile }}
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ steps.commit.outputs.IMAGE_NAME_LOWER }}:testing${{ matrix.tag_suffix }}
          build-args: |
            VERSION=dev-${{ steps.commit.outputs.SHORT_SHA }}
            COMMIT=${{ github.sha }}
          cache-from: type=registry,ref=${{ env.REGISTRY }}/${{ steps.commit.outputs.IMAGE_NAME_LOWER }}:testing${{ matrix.tag_suffix }}
          cache-to: type=inline

      - name: Image digest
        run: |
          echo "✅ ${{ matrix.name }} image pushed to GitHub Container Registry"
          echo "Tag: ${{ env.REGISTRY }}/${{ steps.commit.outputs.IMAGE_NAME_LOWER }}:testing${{ matrix.tag_suffix }}"
